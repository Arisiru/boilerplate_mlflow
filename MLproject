name: mlflow_project_boilerplate

conda_env: conda.yaml

entry_points:
  training:
    parameters:
      config_file: {type: string}
    command: "python entry_training.py {alpha} {l1_ratio} {data}"
  inference:
    command: "python entry_inference.py"
